language: R
cache: packages

r_packages:
  - bookdown

script: bash ./scripts/render.sh

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: docs
  on:
    branch: master
